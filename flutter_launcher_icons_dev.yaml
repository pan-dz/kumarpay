flutter_launcher_icons:
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_foreground: "assets/icon/adaptive432.png" # 自适应图标前景（dev）
  adaptive_icon_background: "#000000" # 自适应图标背景（dev）
  android: true
  ios: true
  remove_alpha_ios: true
  web:
    generate: true
